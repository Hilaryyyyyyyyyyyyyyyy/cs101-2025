include <stdio.h>

int main() {
    FILE* fp;
    int a[3] = {0,1,2};
    char b[3]="ABC";
    float c[3] = {1.1,1.2,1.3};
    
    fp = fopen("a.bin" , "wb");
    int ra[3];
    char rb[3];
    float rc[3];
    
    fwrite(a, sizeof(int), 3, fp);
    fwrite(b, sizeof(char), 3, fp);
    fwrite(c, sizeof(float), 3, fp);
    fclose(fp);
    fread(ra, sizeof(int), 3, fp);
    fread(rb, sizeof(int), 3, fp);
    fread(rc, sizeof(int), 3, fp);
    
    fseek(fp, 0, SEEK_SET);
    
    for (int i=0;i<5;i++){
        printf("%d ", arr_read[i]);
    }
    return 0;
}
