#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    FILE* fp;
    fp = fopen("cj.txt" , "w+");
    fprintf(fp, "This is a openfile example\n");
    fclose(fp);
    return 0;
}
