#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    char str[50];
    int len = 0;
    strcpy(str, "Hello YH!");
    len = strlen(str);
    printf("%s ; %d\n", str, len);
    return 0;
}
