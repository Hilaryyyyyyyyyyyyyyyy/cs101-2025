#include <stdio.h>

int main() {
    int N, number;
    printf("Please enter a number: ");
    scanf("%d", &N);
    for(number = 1; number <= N; number++) {
        if(N % number == 0) {
            printf("%d\n", number);
        }
    }
    return 0;
}
